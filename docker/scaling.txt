COMPOSE="docker run -v /var/run/docker.sock:/var/run/docker.sock -v $PWD:/compose  -e ELASTICSEARCH_INIT_TIMEOUT=1000 -e PROVISION=true -e DOCKER_IP=localhost -w /compose -ti docker/compose:1.6.2 -f dockerfiles/platform/docker-compose-images.yml"

$COMPOSE up

$COMPOSE scale cassandran=1

docker exec -ti platform_cassandra_1 nodetool status
